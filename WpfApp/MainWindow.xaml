<Window x:Class="MVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"      
        mc:Ignorable="d"
         xmlns:local="clr-namespace:MVVM"
        Title="grid_pcap" Height="600" Width="700">
    <Window.Resources>
        
        <Style TargetType="Grid">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
    </Window.Resources>


    <local:CustomDataGrid ItemsSource="{Binding _SnifferData}" AutoGenerateColumns="False" Margin="0,0,0,-6" >
        <DataGrid.Columns>
            <DataGridTextColumn Header="Source_ip"  Width="120" Binding="{Binding Source_ip}"   />
            <DataGridTextColumn Header="Source_port" Width="120" Binding="{Binding Source_port}" />
            <DataGridTextColumn Header="Dest_ip" Width="120" Binding="{Binding Dest_ip}" />
            <DataGridTextColumn Header="Dest_port" Width="120" Binding="{Binding Dest_port}" />
            <DataGridTextColumn Header="Source_mac" Width="120" Binding="{Binding Source_mac}" />
            <DataGridTextColumn Header="Dest_mac" Width="120" Binding="{Binding Dest_mac}" />
        </DataGrid.Columns>
    </local:CustomDataGrid>

    <!--Background="Lavender" ShowGridLines="True"  Margin="0,0,0,0"-->
    <!--<custom:CustomDataGrid ItemsSource="{Binding _SnifferData}" AutoGenerateColumns="False">-->
    <!--<DataGrid  Name="CustomDataGrid" ItemsSource="{Binding _SnifferData}" AutoGenerateColumns="False" Margin="0,0,0,-6">
        --><!--<Border Grid.Column="0" BorderBrush="White" BorderThickness="2">
        </Border>-->
        <!--<ListView Margin="10,10,10,216"  ItemsSource="{Binding SnifferData}" SelectedItem="{Binding SelectedSnifferData}">
            <ListView.View >-->
        <!--<GridView >--><!--
        <DataGrid.Columns>
            <DataGridTextColumn Header="Source_ip"  Width="120" Binding="{Binding Source_ip}"   />
            <DataGridTextColumn Header="Source_port" Width="120" Binding="{Binding Source_port}" />
            <DataGridTextColumn Header="Dest_ip" Width="120" Binding="{Binding Dest_ip}" />
            <DataGridTextColumn Header="Dest_port" Width="120" Binding="{Binding Dest_port}" />
            <DataGridTextColumn Header="Source_mac" Width="120" Binding="{Binding Source_mac}" />
            <DataGridTextColumn Header="Dest_mac" Width="120" Binding="{Binding Dest_mac}" />
        </DataGrid.Columns>
        --><!--</GridView>
            </ListView.View>
        </ListView>--><!--
    </DataGrid>-->
    <!--<CustomControls:CustomDataGrid></CustomControls:CustomDataGrid>-->
    <!--<custom:CustomDataGrid/>-->
</Window>







